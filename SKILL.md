---
name: video-frame-notes
description: 完整处理视频链接或本地视频，逐帧提取并核对画面，结合声音和字幕整理带时间戳的中文笔记；完成后仅保留笔记并清理生成的视频、帧和压缩包。用于用户要求完整过视频或从视频制作详细笔记时。
---

# Video Frame Notes

## 目标

完整处理视频，最终只交付并保留一份可独立阅读、能按时间戳回到原片核对的笔记。逐帧图像、时间戳清单、下载的视频和音频都是临时分析材料；仍须实际处理每个可解码的视频帧，不能用固定间隔截图冒充逐帧结果。

## 获取视频

- 对链接进行的联网访问遵循 `web-access` skill。优先取得用户提供的原视频；记录原链接、标题、时长、视频流、音轨和字幕轨。已有本地文件时直接处理。
- 只处理实际可访问的内容。播放页截图或拖动进度条通常只能离散采样；如果拿不到完整媒体流，说明限制、已完成的部分和缺失区间，不能声称已经逐帧提取或看完整片。需要用户提供源文件时，明确提出。
- 把下载和处理产生的文件放在本次任务专用的临时目录。开始前估算帧数与空间；长视频可分段导出、核对和分析，再清理已处理的临时帧，以限制峰值磁盘占用。空间仍不足时，说明限制并与用户调整处理条件，不要擅自丢帧。

## 逐帧提取与核对

- 使用可靠的视频解码工具（如 `ffprobe` 和 `ffmpeg`）读取实际视频流，保留原始帧率和可变帧率特征；不要通过设定采样帧率改变输出帧数。将每个解码后的呈现帧保存为按顺序编号的图像。
- 临时生成清单，至少包含帧序号、相对时间戳、图像路径；可变帧率视频以每帧实际时间戳为准，不能仅以序号除以标称帧率推算。记录视频流索引、时长、输出格式和提取工具。
- 核对输出文件数与该视频流实际可解码的呈现帧数，检查首尾时间、编号连续性以及解码警告。若有损坏或无法解码的片段，在清单和笔记中标出，不要补造不存在的帧。
- 分段处理时，每段的清单要能从时间戳定位到该段图像，验证完成后再清理。笔记以文字说明画面信息；不要引用清理后会失效的本地帧路径。

## 完整分析

- 覆盖从开头到结尾的音频、字幕和画面。可用转写辅助听取，但要核对专有名词、数字、关键引述和不清楚的语句；不能用页面简介或现成摘要代替看视频。
- 扫描所有导出的帧以发现镜头变化、画面文字、图表、代码、演示步骤和其他视觉状态变化；对不同的信息状态及其前后文进行查看。对快速闪现、字幕切换或细小文字，回到对应连续帧核查。
- 将讲述内容与画面证据按时间对齐。区分视频原话、屏幕文字、自己的概括和推断；对不确定信息保留不确定性，不凭单帧补全过程。
- 按内容组织笔记，保留关键步骤、论点、例子、结论和必要的图表数据。每个重要条目附原视频的时间戳链接；将关键画面的文字、图表和状态变化写入笔记，使它不依赖临时图片。删除重复和无信息的过渡画面描述，但不能跳过有新信息的区间。
- 在重要概念后加入自己的讲解，回答“它是什么意思、为什么这样、实际怎么用”，优先用一个具体小例子或常见误区帮助理解。把这类补充明确标为“我的讲解”或同等醒目的说明，与视频原意分开；例子中的假设条件要写清楚。讲解应准确、简洁，不能把未经核实的延伸知识写成视频内容，也不要逐句复述原文来充数。

## 交付与透明度

默认只提供 Markdown 笔记。笔记开头写明来源、时长和实际覆盖范围；结尾简要列出处理帧数、核对结果、音频或字幕使用情况以及无法确认的内容。长视频可先给摘要，再给按时间段展开的详细笔记。

笔记核对完成后，清理本次任务产生的下载视频、音频、逐帧图像、时间戳清单、转写/OCR 中间结果、日志、压缩包及临时工具文件；失败或中断时也清理已产生的临时材料。最后检查笔记没有指向已删除文件的链接，并确认任务输出中只留下笔记。只清理本次任务明确创建的文件，不删除用户原有的本地视频、其他任务文件或共享缓存。除非用户在当前请求中明确要求保留某种原始材料，否则不把它作为交付物。

“已逐帧提取”只表示每个可解码帧都有文件和时间戳，不等于人工逐张审阅。若只完成采样分析，应明确标为采样，不得称为完整逐帧分析。
